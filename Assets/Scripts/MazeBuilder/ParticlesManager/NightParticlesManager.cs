using UnityEngine;

namespace MazeBuilder.ParticlesManager {
    public class NightParticlesManager : MonoBehaviour {
        public GameObject particles; //Shoud be per Biome particles
        // Use this for initialization
        void Start () {
//            var maze = App.AppManager.Instance.MazeInstance.Maze;
//            for (var i = 0; i < maze.Width; i++) {
//                for (var j = 0; j < maze.Height; j++) {
//                    var coordinate = new Vector3(TransformToWorldCoordinate(i),
//                        GetYForTile(maze.Tiles[i, j].type, maze.Tiles[i, j].Biome ), TransformToWorldCoordinate(j));
//
//                        var tile = Instantiate(particles, coordinate, Quaternion.identity);
//                    }
//                }
            }
	
        // Update is called once per frame
        void Update () {
		
        }
    }
}
